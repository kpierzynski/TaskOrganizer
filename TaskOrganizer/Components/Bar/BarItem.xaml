<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskOrganizer.Components.Bar"
             xmlns:Components="clr-namespace:TaskOrganizer.Components" x:Name="control" x:Class="TaskOrganizer.Components.Bar.BarItem"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Components:Icon x:Name="icon" IsRounded="True" Grid.Column="1" IconClicked="IconClickHandler" HorizontalAlignment="Right"/>
        <TextBlock x:Name="title" Foreground="{StaticResource Font}" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right" Padding="8,0,8,0"/>

    </Grid>
</UserControl>
